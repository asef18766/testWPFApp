<UserControl x:Class="testingApp.DataModel.User.UserInfoViewController"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:testingApp.DataModel.User"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="800">
    <Grid>
        <ListView x:Name="userInfoPanel">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding username}" Header="username" Width="100"></GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding address}" Header="address" Width="400"></GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding age}" Header="age" Width="400"></GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
